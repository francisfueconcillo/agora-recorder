dispatch:

# Matches the path "/fun", but not "/fun2" or "/fun/other"
- url: "*/"
  service: default

- url: "*/api/.*"
  service: agora-api

# - url: "*/api/start"
#   service: agora-api

# - url: "*/api/stop"
#   service: agora-api

# - url: "*/api/records"
#   service: agora-api

# - url: "*/api/query"
#   service: agora-api
